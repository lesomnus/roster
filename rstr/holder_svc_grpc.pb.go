// Code generated by protoc-gen-go-grpc. DO NOT EDIT.
// versions:
// - protoc-gen-go-grpc v1.6.2
// - protoc             (unknown)
// source: roster/payday/holder_svc.g.proto

package rstr

import (
	context "context"
	grpc "google.golang.org/grpc"
	codes "google.golang.org/grpc/codes"
	status "google.golang.org/grpc/status"
)

// This is a compile-time assertion to ensure that this generated file
// is compatible with the grpc package it is being compiled against.
// Requires gRPC-Go v1.64.0 or later.
const _ = grpc.SupportPackageIsVersion9

const (
	HolderService_Add_FullMethodName        = "/roster.HolderService/Add"
	HolderService_Get_FullMethodName        = "/roster.HolderService/Get"
	HolderService_Patch_FullMethodName      = "/roster.HolderService/Patch"
	HolderService_Apply_FullMethodName      = "/roster.HolderService/Apply"
	HolderService_Erase_FullMethodName      = "/roster.HolderService/Erase"
	HolderService_List_FullMethodName       = "/roster.HolderService/List"
	HolderService_Watch_FullMethodName      = "/roster.HolderService/Watch"
	HolderService_Update_FullMethodName     = "/roster.HolderService/Update"
	HolderService_Disable_FullMethodName    = "/roster.HolderService/Disable"
	HolderService_Enable_FullMethodName     = "/roster.HolderService/Enable"
	HolderService_Invalidate_FullMethodName = "/roster.HolderService/Invalidate"
	HolderService_SignsIn_FullMethodName    = "/roster.HolderService/SignsIn"
	HolderService_RevokeKey_FullMethodName  = "/roster.HolderService/RevokeKey"
	HolderService_Reaches_FullMethodName    = "/roster.HolderService/Reaches"
)

// HolderServiceClient is the client API for HolderService service.
//
// For semantics around ctx use and closing/ending streaming RPCs, please refer to https://pkg.go.dev/google.golang.org/grpc/?tab=doc#ClientConn.NewStream.
type HolderServiceClient interface {
	// Add creates a new Holder
	Add(ctx context.Context, in *HolderAddRequest, opts ...grpc.CallOption) (*Holder, error)
	// Get retrieves a Holder
	Get(ctx context.Context, in *HolderGetRequest, opts ...grpc.CallOption) (*Holder, error)
	// Patch updates an existing Holder
	Patch(ctx context.Context, in *HolderPatchRequest, opts ...grpc.CallOption) (*Holder, error)
	// Apply applies a patch document to an existing Holder
	Apply(ctx context.Context, in *HolderApplyRequest, opts ...grpc.CallOption) (*Holder, error)
	// Erase deletes a Holder
	Erase(ctx context.Context, in *HolderRef, opts ...grpc.CallOption) (*HolderEraseResponse, error)
	// List reads Holders a page at a time.
	List(ctx context.Context, in *HolderListRequest, opts ...grpc.CallOption) (*HolderListResponse, error)
	// Watch the Holders this caller may see, as they are now and as they change.
	//
	// What arrives is **state and never a delta**, so a client converges rather
	// than replays: it keeps what it was last told about a row and replaces it.
	// An event it did not receive is one it does not need, since the next one
	// about that row carries the whole of it.
	//
	// The first message is everything that matches right now, so a client does
	// not have to List first and race the subscription. A row may arrive twice --
	// once in that first message and once as a change that happened while it was
	// being read -- and that is harmless for the same reason.
	Watch(ctx context.Context, in *HolderWatchRequest, opts ...grpc.CallOption) (grpc.ServerStreamingClient[HolderWatchResponse], error)
	// Update is the narrow write, and the one a caller is given.
	//
	// `Patch` and `Apply` write anything the schema holds, which is why they are
	// closed at the transport unless a deployment says otherwise -- *what a
	// caller may change, and under what conditions, is not something a general
	// write can be told*.
	//
	// This can be told. Two fields, both of them things a holder carries about
	// itself and neither of them anything the wall, the trail or a permission
	// reads: what a person is called, and whatever the app that put it there
	// keeps. Nothing here can move somebody between tenants, rename them into
	// somebody else's alias, or change what they may do.
	//
	// It is on `HolderService` rather than in a service of its own because it is
	// a write on a holder, and a second service would be one more name for the
	// same rows. The overlay mechanism exists for exactly this and nothing had
	// used it.
	Update(ctx context.Context, in *HolderUpdateRequest, opts ...grpc.CallOption) (*Holder, error)
	// Disable stops somebody signing in, and leaves their rows where they are.
	//
	// Its own method rather than a field on `Update`, because it is not a thing
	// a holder carries about itself -- it is somebody else's decision about them,
	// and roles are lists of methods, so a separate name is the only way a
	// deployment can grant one without granting the other.
	Disable(ctx context.Context, in *HolderDisableRequest, opts ...grpc.CallOption) (*Holder, error)
	// Enable is the other direction, and is a second method for the same reason
	// the first is one: what a role may do is what it names.
	Enable(ctx context.Context, in *HolderEnableRequest, opts ...grpc.CallOption) (*Holder, error)
	// Invalidate voids everything issued before now.
	//
	// No time is taken and none can be. A caller who could write an older value
	// could un-revoke; one who could write a future value could shut somebody out
	// of a deployment with no way to say so. The server stamps it, which is also
	// what makes it monotonic, which is what makes a duplicate a no-op and a
	// missed message a matter of latency rather than correctness.
	//
	// There is no undo, by construction. That is the point rather than an
	// omission: this is what a password reset and a "sign me out everywhere"
	// button both do, and a credential that can be brought back is not revoked.
	Invalidate(ctx context.Context, in *HolderInvalidateRequest, opts ...grpc.CallOption) (*Holder, error)
	// SignsIn answers how somebody signs in, without the verifier.
	//
	// # Why it cannot be read any other way
	//
	// `CredentialService` is not registered at all, because its generated `Get`
	// answers with the verifier (D13). And `IdentityService` narrows by the
	// **tenant**, so an operator's list of one person's identities is that whole
	// tenant's, filtered in the app -- which is the leak D17 named and D23 exists
	// to remove.
	//
	// # Why it is not MeService
	//
	// `MeService` takes nothing, which is the property that makes it safe to read
	// unwalled and the reason `cmd.Policy` waives a binding for it. This takes a
	// subject, so it is the ordinary question with the ordinary answer: behind
	// the wall, and needing a role that names it.
	//
	// It is on `HolderService` and not a service of its own for `Update`'s
	// reason: it is a read of a holder, and a second service would be a second
	// name for the same rows.
	SignsIn(ctx context.Context, in *HolderSignsInRequest, opts ...grpc.CallOption) (*HolderSignsInResponse, error)
	// RevokeKey ends one key of one person's.
	//
	// Beside `SignsIn` for the same reason it is: `ApiKeyService` is unregistered
	// everywhere, so there is no served road to a key row at all -- and a screen
	// that lists somebody's keys and cannot end one is a screen that sends an
	// operator to a shell, which is what `roster key revoke` is and which reaches
	// only the **deployment's** own `rk_` keys.
	//
	// A *which* within a *whose*: the reference says the person, the identifier
	// says the key, and one that is not theirs is `NotFound`. So this cannot be
	// used to ask whether a key exists, and cannot be pointed past the wall the
	// reference is narrowed by.
	//
	// Its own method rather than a field on `Update`, for the reason `Disable`
	// and `Enable` are two: a role is a list of methods, so a deployment can only
	// hand out what it can name -- and *may see how somebody signs in* is a
	// different grant from *may take a way in away*.
	RevokeKey(ctx context.Context, in *HolderRevokeKeyRequest, opts ...grpc.CallOption) (*HolderRevokeKeyResponse, error)
	// Reaches is what somebody may call, as the gate would decide it -- the same
	// answer `MeService.Get` gives about the caller, about somebody else.
	//
	// # Why it is the policy's function and not a read of the parts
	//
	// Every part is readable: their bindings, the groups carrying bindings, the
	// roles held in teams. What is not readable is the **union**, and a console
	// that added the parts up would be a second implementation of `gate.Policy`,
	// drifting from the one that enforces it -- `cmd/policy.go` records three
	// readers of one set disagreeing. So this asks the function the gate asks,
	// handed to the layer as `core.Rules.Held`, and answers with **patterns**
	// rather than an expansion, for `MeService.Get`'s reason: an expansion is the
	// methods that exist in whichever replica answered.
	//
	// # What it is not guarded by, and why
	//
	// The wall, and nothing else. A role naming this reads anybody's reach in the
	// tenant, which is RBAC as it is (CLAUDE.md, *no self-only twin of a verb*)
	// -- and it reveals nothing `BindingService.List`, `GroupMembershipService.
	// List` and `TeamMembershipService.List` do not already answer to the same
	// caller, only added up. A rule that narrowed it by the caller's own reach
	// would be an object rule over a read, which is a shape roster has not taken
	// and does not need here.
	Reaches(ctx context.Context, in *HolderReachesRequest, opts ...grpc.CallOption) (*HolderReachesResponse, error)
}

type holderServiceClient struct {
	cc grpc.ClientConnInterface
}

func NewHolderServiceClient(cc grpc.ClientConnInterface) HolderServiceClient {
	return &holderServiceClient{cc}
}

func (c *holderServiceClient) Add(ctx context.Context, in *HolderAddRequest, opts ...grpc.CallOption) (*Holder, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(Holder)
	err := c.cc.Invoke(ctx, HolderService_Add_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *holderServiceClient) Get(ctx context.Context, in *HolderGetRequest, opts ...grpc.CallOption) (*Holder, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(Holder)
	err := c.cc.Invoke(ctx, HolderService_Get_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *holderServiceClient) Patch(ctx context.Context, in *HolderPatchRequest, opts ...grpc.CallOption) (*Holder, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(Holder)
	err := c.cc.Invoke(ctx, HolderService_Patch_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *holderServiceClient) Apply(ctx context.Context, in *HolderApplyRequest, opts ...grpc.CallOption) (*Holder, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(Holder)
	err := c.cc.Invoke(ctx, HolderService_Apply_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *holderServiceClient) Erase(ctx context.Context, in *HolderRef, opts ...grpc.CallOption) (*HolderEraseResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(HolderEraseResponse)
	err := c.cc.Invoke(ctx, HolderService_Erase_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *holderServiceClient) List(ctx context.Context, in *HolderListRequest, opts ...grpc.CallOption) (*HolderListResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(HolderListResponse)
	err := c.cc.Invoke(ctx, HolderService_List_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *holderServiceClient) Watch(ctx context.Context, in *HolderWatchRequest, opts ...grpc.CallOption) (grpc.ServerStreamingClient[HolderWatchResponse], error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	stream, err := c.cc.NewStream(ctx, &HolderService_ServiceDesc.Streams[0], HolderService_Watch_FullMethodName, cOpts...)
	if err != nil {
		return nil, err
	}
	x := &grpc.GenericClientStream[HolderWatchRequest, HolderWatchResponse]{ClientStream: stream}
	if err := x.ClientStream.SendMsg(in); err != nil {
		return nil, err
	}
	if err := x.ClientStream.CloseSend(); err != nil {
		return nil, err
	}
	return x, nil
}

// This type alias is provided for backwards compatibility with existing code that references the prior non-generic stream type by name.
type HolderService_WatchClient = grpc.ServerStreamingClient[HolderWatchResponse]

func (c *holderServiceClient) Update(ctx context.Context, in *HolderUpdateRequest, opts ...grpc.CallOption) (*Holder, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(Holder)
	err := c.cc.Invoke(ctx, HolderService_Update_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *holderServiceClient) Disable(ctx context.Context, in *HolderDisableRequest, opts ...grpc.CallOption) (*Holder, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(Holder)
	err := c.cc.Invoke(ctx, HolderService_Disable_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *holderServiceClient) Enable(ctx context.Context, in *HolderEnableRequest, opts ...grpc.CallOption) (*Holder, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(Holder)
	err := c.cc.Invoke(ctx, HolderService_Enable_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *holderServiceClient) Invalidate(ctx context.Context, in *HolderInvalidateRequest, opts ...grpc.CallOption) (*Holder, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(Holder)
	err := c.cc.Invoke(ctx, HolderService_Invalidate_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *holderServiceClient) SignsIn(ctx context.Context, in *HolderSignsInRequest, opts ...grpc.CallOption) (*HolderSignsInResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(HolderSignsInResponse)
	err := c.cc.Invoke(ctx, HolderService_SignsIn_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *holderServiceClient) RevokeKey(ctx context.Context, in *HolderRevokeKeyRequest, opts ...grpc.CallOption) (*HolderRevokeKeyResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(HolderRevokeKeyResponse)
	err := c.cc.Invoke(ctx, HolderService_RevokeKey_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *holderServiceClient) Reaches(ctx context.Context, in *HolderReachesRequest, opts ...grpc.CallOption) (*HolderReachesResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(HolderReachesResponse)
	err := c.cc.Invoke(ctx, HolderService_Reaches_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

// HolderServiceServer is the server API for HolderService service.
// All implementations must embed UnimplementedHolderServiceServer
// for forward compatibility.
type HolderServiceServer interface {
	// Add creates a new Holder
	Add(context.Context, *HolderAddRequest) (*Holder, error)
	// Get retrieves a Holder
	Get(context.Context, *HolderGetRequest) (*Holder, error)
	// Patch updates an existing Holder
	Patch(context.Context, *HolderPatchRequest) (*Holder, error)
	// Apply applies a patch document to an existing Holder
	Apply(context.Context, *HolderApplyRequest) (*Holder, error)
	// Erase deletes a Holder
	Erase(context.Context, *HolderRef) (*HolderEraseResponse, error)
	// List reads Holders a page at a time.
	List(context.Context, *HolderListRequest) (*HolderListResponse, error)
	// Watch the Holders this caller may see, as they are now and as they change.
	//
	// What arrives is **state and never a delta**, so a client converges rather
	// than replays: it keeps what it was last told about a row and replaces it.
	// An event it did not receive is one it does not need, since the next one
	// about that row carries the whole of it.
	//
	// The first message is everything that matches right now, so a client does
	// not have to List first and race the subscription. A row may arrive twice --
	// once in that first message and once as a change that happened while it was
	// being read -- and that is harmless for the same reason.
	Watch(*HolderWatchRequest, grpc.ServerStreamingServer[HolderWatchResponse]) error
	// Update is the narrow write, and the one a caller is given.
	//
	// `Patch` and `Apply` write anything the schema holds, which is why they are
	// closed at the transport unless a deployment says otherwise -- *what a
	// caller may change, and under what conditions, is not something a general
	// write can be told*.
	//
	// This can be told. Two fields, both of them things a holder carries about
	// itself and neither of them anything the wall, the trail or a permission
	// reads: what a person is called, and whatever the app that put it there
	// keeps. Nothing here can move somebody between tenants, rename them into
	// somebody else's alias, or change what they may do.
	//
	// It is on `HolderService` rather than in a service of its own because it is
	// a write on a holder, and a second service would be one more name for the
	// same rows. The overlay mechanism exists for exactly this and nothing had
	// used it.
	Update(context.Context, *HolderUpdateRequest) (*Holder, error)
	// Disable stops somebody signing in, and leaves their rows where they are.
	//
	// Its own method rather than a field on `Update`, because it is not a thing
	// a holder carries about itself -- it is somebody else's decision about them,
	// and roles are lists of methods, so a separate name is the only way a
	// deployment can grant one without granting the other.
	Disable(context.Context, *HolderDisableRequest) (*Holder, error)
	// Enable is the other direction, and is a second method for the same reason
	// the first is one: what a role may do is what it names.
	Enable(context.Context, *HolderEnableRequest) (*Holder, error)
	// Invalidate voids everything issued before now.
	//
	// No time is taken and none can be. A caller who could write an older value
	// could un-revoke; one who could write a future value could shut somebody out
	// of a deployment with no way to say so. The server stamps it, which is also
	// what makes it monotonic, which is what makes a duplicate a no-op and a
	// missed message a matter of latency rather than correctness.
	//
	// There is no undo, by construction. That is the point rather than an
	// omission: this is what a password reset and a "sign me out everywhere"
	// button both do, and a credential that can be brought back is not revoked.
	Invalidate(context.Context, *HolderInvalidateRequest) (*Holder, error)
	// SignsIn answers how somebody signs in, without the verifier.
	//
	// # Why it cannot be read any other way
	//
	// `CredentialService` is not registered at all, because its generated `Get`
	// answers with the verifier (D13). And `IdentityService` narrows by the
	// **tenant**, so an operator's list of one person's identities is that whole
	// tenant's, filtered in the app -- which is the leak D17 named and D23 exists
	// to remove.
	//
	// # Why it is not MeService
	//
	// `MeService` takes nothing, which is the property that makes it safe to read
	// unwalled and the reason `cmd.Policy` waives a binding for it. This takes a
	// subject, so it is the ordinary question with the ordinary answer: behind
	// the wall, and needing a role that names it.
	//
	// It is on `HolderService` and not a service of its own for `Update`'s
	// reason: it is a read of a holder, and a second service would be a second
	// name for the same rows.
	SignsIn(context.Context, *HolderSignsInRequest) (*HolderSignsInResponse, error)
	// RevokeKey ends one key of one person's.
	//
	// Beside `SignsIn` for the same reason it is: `ApiKeyService` is unregistered
	// everywhere, so there is no served road to a key row at all -- and a screen
	// that lists somebody's keys and cannot end one is a screen that sends an
	// operator to a shell, which is what `roster key revoke` is and which reaches
	// only the **deployment's** own `rk_` keys.
	//
	// A *which* within a *whose*: the reference says the person, the identifier
	// says the key, and one that is not theirs is `NotFound`. So this cannot be
	// used to ask whether a key exists, and cannot be pointed past the wall the
	// reference is narrowed by.
	//
	// Its own method rather than a field on `Update`, for the reason `Disable`
	// and `Enable` are two: a role is a list of methods, so a deployment can only
	// hand out what it can name -- and *may see how somebody signs in* is a
	// different grant from *may take a way in away*.
	RevokeKey(context.Context, *HolderRevokeKeyRequest) (*HolderRevokeKeyResponse, error)
	// Reaches is what somebody may call, as the gate would decide it -- the same
	// answer `MeService.Get` gives about the caller, about somebody else.
	//
	// # Why it is the policy's function and not a read of the parts
	//
	// Every part is readable: their bindings, the groups carrying bindings, the
	// roles held in teams. What is not readable is the **union**, and a console
	// that added the parts up would be a second implementation of `gate.Policy`,
	// drifting from the one that enforces it -- `cmd/policy.go` records three
	// readers of one set disagreeing. So this asks the function the gate asks,
	// handed to the layer as `core.Rules.Held`, and answers with **patterns**
	// rather than an expansion, for `MeService.Get`'s reason: an expansion is the
	// methods that exist in whichever replica answered.
	//
	// # What it is not guarded by, and why
	//
	// The wall, and nothing else. A role naming this reads anybody's reach in the
	// tenant, which is RBAC as it is (CLAUDE.md, *no self-only twin of a verb*)
	// -- and it reveals nothing `BindingService.List`, `GroupMembershipService.
	// List` and `TeamMembershipService.List` do not already answer to the same
	// caller, only added up. A rule that narrowed it by the caller's own reach
	// would be an object rule over a read, which is a shape roster has not taken
	// and does not need here.
	Reaches(context.Context, *HolderReachesRequest) (*HolderReachesResponse, error)
	mustEmbedUnimplementedHolderServiceServer()
}

// UnimplementedHolderServiceServer must be embedded to have
// forward compatible implementations.
//
// NOTE: this should be embedded by value instead of pointer to avoid a nil
// pointer dereference when methods are called.
type UnimplementedHolderServiceServer struct{}

func (UnimplementedHolderServiceServer) Add(context.Context, *HolderAddRequest) (*Holder, error) {
	return nil, status.Error(codes.Unimplemented, "method Add not implemented")
}
func (UnimplementedHolderServiceServer) Get(context.Context, *HolderGetRequest) (*Holder, error) {
	return nil, status.Error(codes.Unimplemented, "method Get not implemented")
}
func (UnimplementedHolderServiceServer) Patch(context.Context, *HolderPatchRequest) (*Holder, error) {
	return nil, status.Error(codes.Unimplemented, "method Patch not implemented")
}
func (UnimplementedHolderServiceServer) Apply(context.Context, *HolderApplyRequest) (*Holder, error) {
	return nil, status.Error(codes.Unimplemented, "method Apply not implemented")
}
func (UnimplementedHolderServiceServer) Erase(context.Context, *HolderRef) (*HolderEraseResponse, error) {
	return nil, status.Error(codes.Unimplemented, "method Erase not implemented")
}
func (UnimplementedHolderServiceServer) List(context.Context, *HolderListRequest) (*HolderListResponse, error) {
	return nil, status.Error(codes.Unimplemented, "method List not implemented")
}
func (UnimplementedHolderServiceServer) Watch(*HolderWatchRequest, grpc.ServerStreamingServer[HolderWatchResponse]) error {
	return status.Error(codes.Unimplemented, "method Watch not implemented")
}
func (UnimplementedHolderServiceServer) Update(context.Context, *HolderUpdateRequest) (*Holder, error) {
	return nil, status.Error(codes.Unimplemented, "method Update not implemented")
}
func (UnimplementedHolderServiceServer) Disable(context.Context, *HolderDisableRequest) (*Holder, error) {
	return nil, status.Error(codes.Unimplemented, "method Disable not implemented")
}
func (UnimplementedHolderServiceServer) Enable(context.Context, *HolderEnableRequest) (*Holder, error) {
	return nil, status.Error(codes.Unimplemented, "method Enable not implemented")
}
func (UnimplementedHolderServiceServer) Invalidate(context.Context, *HolderInvalidateRequest) (*Holder, error) {
	return nil, status.Error(codes.Unimplemented, "method Invalidate not implemented")
}
func (UnimplementedHolderServiceServer) SignsIn(context.Context, *HolderSignsInRequest) (*HolderSignsInResponse, error) {
	return nil, status.Error(codes.Unimplemented, "method SignsIn not implemented")
}
func (UnimplementedHolderServiceServer) RevokeKey(context.Context, *HolderRevokeKeyRequest) (*HolderRevokeKeyResponse, error) {
	return nil, status.Error(codes.Unimplemented, "method RevokeKey not implemented")
}
func (UnimplementedHolderServiceServer) Reaches(context.Context, *HolderReachesRequest) (*HolderReachesResponse, error) {
	return nil, status.Error(codes.Unimplemented, "method Reaches not implemented")
}
func (UnimplementedHolderServiceServer) mustEmbedUnimplementedHolderServiceServer() {}
func (UnimplementedHolderServiceServer) testEmbeddedByValue()                       {}

// UnsafeHolderServiceServer may be embedded to opt out of forward compatibility for this service.
// Use of this interface is not recommended, as added methods to HolderServiceServer will
// result in compilation errors.
type UnsafeHolderServiceServer interface {
	mustEmbedUnimplementedHolderServiceServer()
}

func RegisterHolderServiceServer(s grpc.ServiceRegistrar, srv HolderServiceServer) {
	// If the following call panics, it indicates UnimplementedHolderServiceServer was
	// embedded by pointer and is nil.  This will cause panics if an
	// unimplemented method is ever invoked, so we test this at initialization
	// time to prevent it from happening at runtime later due to I/O.
	if t, ok := srv.(interface{ testEmbeddedByValue() }); ok {
		t.testEmbeddedByValue()
	}
	s.RegisterService(&HolderService_ServiceDesc, srv)
}

func _HolderService_Add_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(HolderAddRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(HolderServiceServer).Add(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: HolderService_Add_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(HolderServiceServer).Add(ctx, req.(*HolderAddRequest))
	}
	return interceptor(ctx, in, info, handler)
}

func _HolderService_Get_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(HolderGetRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(HolderServiceServer).Get(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: HolderService_Get_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(HolderServiceServer).Get(ctx, req.(*HolderGetRequest))
	}
	return interceptor(ctx, in, info, handler)
}

func _HolderService_Patch_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(HolderPatchRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(HolderServiceServer).Patch(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: HolderService_Patch_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(HolderServiceServer).Patch(ctx, req.(*HolderPatchRequest))
	}
	return interceptor(ctx, in, info, handler)
}

func _HolderService_Apply_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(HolderApplyRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(HolderServiceServer).Apply(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: HolderService_Apply_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(HolderServiceServer).Apply(ctx, req.(*HolderApplyRequest))
	}
	return interceptor(ctx, in, info, handler)
}

func _HolderService_Erase_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(HolderRef)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(HolderServiceServer).Erase(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: HolderService_Erase_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(HolderServiceServer).Erase(ctx, req.(*HolderRef))
	}
	return interceptor(ctx, in, info, handler)
}

func _HolderService_List_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(HolderListRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(HolderServiceServer).List(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: HolderService_List_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(HolderServiceServer).List(ctx, req.(*HolderListRequest))
	}
	return interceptor(ctx, in, info, handler)
}

func _HolderService_Watch_Handler(srv interface{}, stream grpc.ServerStream) error {
	m := new(HolderWatchRequest)
	if err := stream.RecvMsg(m); err != nil {
		return err
	}
	return srv.(HolderServiceServer).Watch(m, &grpc.GenericServerStream[HolderWatchRequest, HolderWatchResponse]{ServerStream: stream})
}

// This type alias is provided for backwards compatibility with existing code that references the prior non-generic stream type by name.
type HolderService_WatchServer = grpc.ServerStreamingServer[HolderWatchResponse]

func _HolderService_Update_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(HolderUpdateRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(HolderServiceServer).Update(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: HolderService_Update_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(HolderServiceServer).Update(ctx, req.(*HolderUpdateRequest))
	}
	return interceptor(ctx, in, info, handler)
}

func _HolderService_Disable_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(HolderDisableRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(HolderServiceServer).Disable(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: HolderService_Disable_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(HolderServiceServer).Disable(ctx, req.(*HolderDisableRequest))
	}
	return interceptor(ctx, in, info, handler)
}

func _HolderService_Enable_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(HolderEnableRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(HolderServiceServer).Enable(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: HolderService_Enable_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(HolderServiceServer).Enable(ctx, req.(*HolderEnableRequest))
	}
	return interceptor(ctx, in, info, handler)
}

func _HolderService_Invalidate_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(HolderInvalidateRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(HolderServiceServer).Invalidate(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: HolderService_Invalidate_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(HolderServiceServer).Invalidate(ctx, req.(*HolderInvalidateRequest))
	}
	return interceptor(ctx, in, info, handler)
}

func _HolderService_SignsIn_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(HolderSignsInRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(HolderServiceServer).SignsIn(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: HolderService_SignsIn_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(HolderServiceServer).SignsIn(ctx, req.(*HolderSignsInRequest))
	}
	return interceptor(ctx, in, info, handler)
}

func _HolderService_RevokeKey_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(HolderRevokeKeyRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(HolderServiceServer).RevokeKey(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: HolderService_RevokeKey_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(HolderServiceServer).RevokeKey(ctx, req.(*HolderRevokeKeyRequest))
	}
	return interceptor(ctx, in, info, handler)
}

func _HolderService_Reaches_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(HolderReachesRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(HolderServiceServer).Reaches(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: HolderService_Reaches_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(HolderServiceServer).Reaches(ctx, req.(*HolderReachesRequest))
	}
	return interceptor(ctx, in, info, handler)
}

// HolderService_ServiceDesc is the grpc.ServiceDesc for HolderService service.
// It's only intended for direct use with grpc.RegisterService,
// and not to be introspected or modified (even as a copy)
var HolderService_ServiceDesc = grpc.ServiceDesc{
	ServiceName: "roster.HolderService",
	HandlerType: (*HolderServiceServer)(nil),
	Methods: []grpc.MethodDesc{
		{
			MethodName: "Add",
			Handler:    _HolderService_Add_Handler,
		},
		{
			MethodName: "Get",
			Handler:    _HolderService_Get_Handler,
		},
		{
			MethodName: "Patch",
			Handler:    _HolderService_Patch_Handler,
		},
		{
			MethodName: "Apply",
			Handler:    _HolderService_Apply_Handler,
		},
		{
			MethodName: "Erase",
			Handler:    _HolderService_Erase_Handler,
		},
		{
			MethodName: "List",
			Handler:    _HolderService_List_Handler,
		},
		{
			MethodName: "Update",
			Handler:    _HolderService_Update_Handler,
		},
		{
			MethodName: "Disable",
			Handler:    _HolderService_Disable_Handler,
		},
		{
			MethodName: "Enable",
			Handler:    _HolderService_Enable_Handler,
		},
		{
			MethodName: "Invalidate",
			Handler:    _HolderService_Invalidate_Handler,
		},
		{
			MethodName: "SignsIn",
			Handler:    _HolderService_SignsIn_Handler,
		},
		{
			MethodName: "RevokeKey",
			Handler:    _HolderService_RevokeKey_Handler,
		},
		{
			MethodName: "Reaches",
			Handler:    _HolderService_Reaches_Handler,
		},
	},
	Streams: []grpc.StreamDesc{
		{
			StreamName:    "Watch",
			Handler:       _HolderService_Watch_Handler,
			ServerStreams: true,
		},
	},
	Metadata: "roster/payday/holder_svc.g.proto",
}
