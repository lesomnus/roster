// Code generated by protoc-gen-go-grpc. DO NOT EDIT.
// versions:
// - protoc-gen-go-grpc v1.6.2
// - protoc             (unknown)
// source: app/front.proto

package rstr

import (
	context "context"
	grpc "google.golang.org/grpc"
	codes "google.golang.org/grpc/codes"
	status "google.golang.org/grpc/status"
)

// This is a compile-time assertion to ensure that this generated file
// is compatible with the grpc package it is being compiled against.
// Requires gRPC-Go v1.64.0 or later.
const _ = grpc.SupportPackageIsVersion9

const (
	FrontService_WhoseHost_FullMethodName = "/roster.FrontService/WhoseHost"
	FrontService_WhereFrom_FullMethodName = "/roster.FrontService/WhereFrom"
)

// FrontServiceClient is the client API for FrontService service.
//
// For semantics around ctx use and closing/ending streaming RPCs, please refer to https://pkg.go.dev/google.golang.org/grpc/?tab=doc#ClientConn.NewStream.
//
// FrontService is what a front door asks before it knows anything.
//
// # Why it is not `HostService.Get`
//
// The generated one exists and is registered and is the right thing for an
// operator managing their own names -- behind the wall, answering with rows.
// This is the other read, and it differs in the one way that decides everything
// else: it happens **before anybody has been resolved to a tenant.**
//
// That is `vouch.Verify`'s situation exactly, and this takes the same shape and
// for the same stated reason -- *working out who somebody is cannot require
// knowing who they are.* So it reads the server the wall was never installed
// on, and what keeps that honest is that it answers with **one identifier and
// nothing else**. There is no row here to be given away, no `Select` to get
// wrong, and nothing that could be pointed at anything but a name somebody
// typed into DNS.
//
// # Neither half is public
//
// Like `VouchService`, and for the same reason. The person arriving has no
// credential -- that is what they are asking for -- but they are not the
// caller. The caller is a front door, holding a key, long before any of this.
type FrontServiceClient interface {
	// WhoseHost answers which tenant serves a name.
	WhoseHost(ctx context.Context, in *FrontWhoseHostRequest, opts ...grpc.CallOption) (*FrontWhoseHostResponse, error)
	// WhereFrom answers where the people at an address authenticate.
	//
	// Home-realm discovery, and it hangs off the **domain** rather than off a
	// person: answered per person it is the account-enumeration oracle D21 spent
	// a condition avoiding, since typing an address and being told a provider
	// says whether that account is here. The answer for `nobody@acme.com` is the
	// answer for everybody at acme.
	WhereFrom(ctx context.Context, in *FrontWhereFromRequest, opts ...grpc.CallOption) (*FrontWhereFromResponse, error)
}

type frontServiceClient struct {
	cc grpc.ClientConnInterface
}

func NewFrontServiceClient(cc grpc.ClientConnInterface) FrontServiceClient {
	return &frontServiceClient{cc}
}

func (c *frontServiceClient) WhoseHost(ctx context.Context, in *FrontWhoseHostRequest, opts ...grpc.CallOption) (*FrontWhoseHostResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(FrontWhoseHostResponse)
	err := c.cc.Invoke(ctx, FrontService_WhoseHost_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *frontServiceClient) WhereFrom(ctx context.Context, in *FrontWhereFromRequest, opts ...grpc.CallOption) (*FrontWhereFromResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(FrontWhereFromResponse)
	err := c.cc.Invoke(ctx, FrontService_WhereFrom_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

// FrontServiceServer is the server API for FrontService service.
// All implementations must embed UnimplementedFrontServiceServer
// for forward compatibility.
//
// FrontService is what a front door asks before it knows anything.
//
// # Why it is not `HostService.Get`
//
// The generated one exists and is registered and is the right thing for an
// operator managing their own names -- behind the wall, answering with rows.
// This is the other read, and it differs in the one way that decides everything
// else: it happens **before anybody has been resolved to a tenant.**
//
// That is `vouch.Verify`'s situation exactly, and this takes the same shape and
// for the same stated reason -- *working out who somebody is cannot require
// knowing who they are.* So it reads the server the wall was never installed
// on, and what keeps that honest is that it answers with **one identifier and
// nothing else**. There is no row here to be given away, no `Select` to get
// wrong, and nothing that could be pointed at anything but a name somebody
// typed into DNS.
//
// # Neither half is public
//
// Like `VouchService`, and for the same reason. The person arriving has no
// credential -- that is what they are asking for -- but they are not the
// caller. The caller is a front door, holding a key, long before any of this.
type FrontServiceServer interface {
	// WhoseHost answers which tenant serves a name.
	WhoseHost(context.Context, *FrontWhoseHostRequest) (*FrontWhoseHostResponse, error)
	// WhereFrom answers where the people at an address authenticate.
	//
	// Home-realm discovery, and it hangs off the **domain** rather than off a
	// person: answered per person it is the account-enumeration oracle D21 spent
	// a condition avoiding, since typing an address and being told a provider
	// says whether that account is here. The answer for `nobody@acme.com` is the
	// answer for everybody at acme.
	WhereFrom(context.Context, *FrontWhereFromRequest) (*FrontWhereFromResponse, error)
	mustEmbedUnimplementedFrontServiceServer()
}

// UnimplementedFrontServiceServer must be embedded to have
// forward compatible implementations.
//
// NOTE: this should be embedded by value instead of pointer to avoid a nil
// pointer dereference when methods are called.
type UnimplementedFrontServiceServer struct{}

func (UnimplementedFrontServiceServer) WhoseHost(context.Context, *FrontWhoseHostRequest) (*FrontWhoseHostResponse, error) {
	return nil, status.Error(codes.Unimplemented, "method WhoseHost not implemented")
}
func (UnimplementedFrontServiceServer) WhereFrom(context.Context, *FrontWhereFromRequest) (*FrontWhereFromResponse, error) {
	return nil, status.Error(codes.Unimplemented, "method WhereFrom not implemented")
}
func (UnimplementedFrontServiceServer) mustEmbedUnimplementedFrontServiceServer() {}
func (UnimplementedFrontServiceServer) testEmbeddedByValue()                      {}

// UnsafeFrontServiceServer may be embedded to opt out of forward compatibility for this service.
// Use of this interface is not recommended, as added methods to FrontServiceServer will
// result in compilation errors.
type UnsafeFrontServiceServer interface {
	mustEmbedUnimplementedFrontServiceServer()
}

func RegisterFrontServiceServer(s grpc.ServiceRegistrar, srv FrontServiceServer) {
	// If the following call panics, it indicates UnimplementedFrontServiceServer was
	// embedded by pointer and is nil.  This will cause panics if an
	// unimplemented method is ever invoked, so we test this at initialization
	// time to prevent it from happening at runtime later due to I/O.
	if t, ok := srv.(interface{ testEmbeddedByValue() }); ok {
		t.testEmbeddedByValue()
	}
	s.RegisterService(&FrontService_ServiceDesc, srv)
}

func _FrontService_WhoseHost_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(FrontWhoseHostRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(FrontServiceServer).WhoseHost(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: FrontService_WhoseHost_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(FrontServiceServer).WhoseHost(ctx, req.(*FrontWhoseHostRequest))
	}
	return interceptor(ctx, in, info, handler)
}

func _FrontService_WhereFrom_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(FrontWhereFromRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(FrontServiceServer).WhereFrom(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: FrontService_WhereFrom_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(FrontServiceServer).WhereFrom(ctx, req.(*FrontWhereFromRequest))
	}
	return interceptor(ctx, in, info, handler)
}

// FrontService_ServiceDesc is the grpc.ServiceDesc for FrontService service.
// It's only intended for direct use with grpc.RegisterService,
// and not to be introspected or modified (even as a copy)
var FrontService_ServiceDesc = grpc.ServiceDesc{
	ServiceName: "roster.FrontService",
	HandlerType: (*FrontServiceServer)(nil),
	Methods: []grpc.MethodDesc{
		{
			MethodName: "WhoseHost",
			Handler:    _FrontService_WhoseHost_Handler,
		},
		{
			MethodName: "WhereFrom",
			Handler:    _FrontService_WhereFrom_Handler,
		},
	},
	Streams:  []grpc.StreamDesc{},
	Metadata: "app/front.proto",
}
